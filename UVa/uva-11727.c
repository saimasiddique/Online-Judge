#include<stdio.h>
int main()
{
    long long int a,b,c,x,y;
    while(scanf("%lld",&y)==1)
        for(x=1;x<=y;x++)
    {
        scanf("%lld %lld %lld",&a,&b,&c);
        if(a>=b && a<=c)
        {
            if(b>=c) printf("Case")
        }
    }
}
