#include<stdio.h>
#include<string.h>
int main()
{
    char jump[100];
    int i,len;
    gets(jump);
    len=strlen(jump);
    for(i=0;i<len;i++)
    {
        if(jump[i]=='A' || jump[i]=='E' || jump[i]=='I' || jump[i]=='O' || jump[i]=='U' || jump[i]=='Y')
    }
}
