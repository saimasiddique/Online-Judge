#include<bits/bits/stdc++.h>
using namespace std;

int main()
{
    int t,i,l,j;
    char str[200];
    while(scanf("%d",&t)==1)
    {
        while(t--)
        {
            gets(str);
            l=strlen(str);
            s=0;
            e=l-1;
            while(s<=e)
            {
                if(str[])
            }
        }
    }
}
